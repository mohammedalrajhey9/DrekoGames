const fs=require('fs');const s=fs.readFileSync('src/App.tsx','utf8');let bal=0;let lastOpenIndex=-1;for(let i=0;i<s.length;i++){const ch=s[i];if(ch==='{'){bal++;lastOpenIndex=i}else if(ch==='}')bal--}if(bal>0){const lines=s.slice(0,lastOpenIndex).split('\n');console.log('Last open at index',lastOpenIndex,'line',lines.length);console.log(s.split('\n').slice(Math.max(0,lines.length-4),lines.length+2).map((l,idx)=>`${Math.max(1,lines.length-4)+idx}: ${l}`).join('\n'))}else{console.log('All braces matched')}
